    #  Load required packages for PopR shiny 
    require(shiny)
    require(stringr)
    require(ggplot2)
    require(xts)
    require(shinysky)  
    require(R2jags)
	require(rmarkdown)
	require(knitr)
    require(dplyr)
	require(RCurl)
	require(googleVis)
	require(mailR)
	require(sendmailR)  # for N-mixture output emails